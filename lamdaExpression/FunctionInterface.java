package java8features.lamdaExpression;

public interface FunctionInterface {
    public String printMessage(); // no requires to write abstarct keyword by default every public methods are abstract in interface.
}
